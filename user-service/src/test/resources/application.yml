spring:
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

eureka:
  client:
    enabled: false

jwt:
  secret: a-very-long-and-secure-random-string-for-jwt-secret-key-at-least-64-chars
  expiration: 3600
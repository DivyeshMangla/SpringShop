server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native # Use the native file system backend
  cloud:
    config:
      server:
        native:
          # Point to a 'configs' directory inside our project
          search-locations: file:./configs
